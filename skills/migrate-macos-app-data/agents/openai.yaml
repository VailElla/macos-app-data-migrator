interface:
  display_name: "macOS 程序与数据迁移 / App Data Migrator"
  short_description: "Non-destructive macOS migration / 无损外接迁移"
  default_prompt: "Use $migrate-macos-app-data / 使用 $migrate-macos-app-data，在不删除内置源文件、不使用 sudo、且不把临时迁移负载写入内置盘的前提下，把一个明确的 macOS 程序或数据目录复制、完整校验并通过访达交接到外接 APFS。"
